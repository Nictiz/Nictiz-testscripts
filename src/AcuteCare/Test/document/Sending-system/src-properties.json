
  { "goal" : "${goal}",
    "fhirVersion" : "${fhir.version}",
    "informationStandard" : "${informationStandard}",
    "usecase" : "${usecase}",
    "category" : "document",
    "role" : 
    { "name" : "Sending-system",
      "description" : "HgActorSenderAmbulanceHAP - Ambulance management system (AMBS) that produces and transmits the Ambulance referral to the HIS/HAPIS." },

    "serverAlias" : "${serverAlias}" }
<?xml version="1.0" encoding="UTF-8"?>
<!--
    Check a XIS to serve the required resources using a search operation using additional search parameters.
    @param resource - The resource to search for.
    @param params - Additional parameters to add to the search URL.
-->
<nts:component xmlns="http://hl7.org/fhir" xmlns:nts="http://nictiz.nl/xsl/testscript">
    <action>
        <operation>
            <type>
                <system value="http://hl7.org/fhir/testscript-operation-codes"/>
                <code value="search"/>
            </type>
            <resource value="{$resource}"/>
            <description value="Test XIS server to serve {$resource} resources."/>
            <destination value="1"/>
            <origin value="1"/>
            <params value="{$params}"/>
            <requestHeader>
                <field value="Authorization"/>
                <value value="${patient-token-id}"/>
            </requestHeader>
            <nts:include value="medmij/requestHeader-MedMijRequestId" scope="common"/>
        </operation>
    </action>
</nts:component>
<?xml version="1.0" encoding="UTF-8"?>
<!--
  Test that the authorization header is present in the request. Expects the variable "patient-token-id" to be set.
-->
<nts:component xmlns="http://hl7.org/fhir" xmlns:nts="http://nictiz.nl/xsl/testscript">
    <action>
        <assert>
            <description value="Confirm that HTTP header Authorization contains the patient token ${patient-token-id}"/>
            <direction value="request"/>
            <headerField value="Authorization"/>
            <value value="${patient-token-id}"/>
        </assert>
    </action>
</nts:component>


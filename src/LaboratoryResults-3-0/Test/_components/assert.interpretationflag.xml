<?xml version="1.0" encoding="UTF-8"?>
<nts:component xmlns="http://hl7.org/fhir" xmlns:nts="http://nictiz.nl/xsl/testscript">
    <nts:parameter name="warningOnly" value="false"/>
    <action>
        <assert>
            <description
                value="Confirm that the Bundle contains a(n) {$resource} with the interpretationflag {$code} with system {$system}."/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).interpretation.where(coding.code='{$code}' and coding.system='{$system}' and coding.display.exists()).exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
    <action>
        <assert>
            <description
                value="Confirm that the Bundle contains a(n) {$resource} with the interpretationflag {$code} with system {$system}."/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).ivalueCodeableConcept.coding.where(coding.code='{$code}' and coding.system='{$system}' and coding.display.exists()).exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
</nts:component>


<?xml version="1.0" encoding="UTF-8"?>
<nts:component xmlns="http://hl7.org/fhir" xmlns:nts="http://nictiz.nl/xsl/testscript">
    <nts:parameter name="warningOnly" value="true"/>
    <action>
        <assert>
            <description
                value="Confirm that the Bundle contains a(n) {$resource} with a value of {$quantity}, and system {$system} and a unit."/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).valueQuantity.where(value='{$quantity}' and system='{$system}' and code='{$code}' and unit.exists())"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
   
</nts:component>
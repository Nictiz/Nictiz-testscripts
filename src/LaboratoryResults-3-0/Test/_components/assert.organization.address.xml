<?xml version="1.0" encoding="UTF-8"?>
<nts:component xmlns="http://hl7.org/fhir" xmlns:nts="http://nictiz.nl/xsl/testscript">
    <nts:parameter name="warningOnly" value="false"/>
    <action>
        <assert>
            <description
                value="Confirm that Bundle contains the right address (street) for the {$resource}"/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).address.line.extension.where(url='{$urlstreet}' and value='{$street}').exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
    
    <action>
        <assert>
            <description
                value="Confirm that Bundle contains the right address (housenumber) for the {$resource}"/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).address.line.extension.where(url='{$urlnumber}' and value ='{$number}').exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
    
    <action>
        <assert>
            <description
                value="Confirm that Bundle contains the right address (city and postalcode) for the {$resource}"/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).address.where(city='{$city}' and postalCode='{$postalcode}').exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
    
    <action>
        <assert>
            <description
                value="Confirm that Bundle contains the right address (type) for the {$resource}"/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).address.extension.where(url='{$urltype}').value.coding.where(system='{$system}' and code='{$code}' and display='{$display}').exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
    
    <!--<action>
        <assert>
            <description
                value="Confirm that Bundle contains the right address (type) for the {$resource}"/>
            <direction value="{$direction}"/>
            <expression value="Bundle.entry.resource.as({$resource}).address.extension.where(url='{$urltype}').exists()"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>-->
</nts:component>
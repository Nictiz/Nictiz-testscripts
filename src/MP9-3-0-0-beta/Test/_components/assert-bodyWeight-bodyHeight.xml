<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="../../../general/schematron/NictizTestScript.sch" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>
<nts:component xmlns="http://hl7.org/fhir" xmlns:nts="http://nictiz.nl/xsl/testscript">
    <nts:parameter name="warningOnly" value="false"/>
    
    <action>
        <assert>
            <description value="Check that the transaction Bundle contains exactly 1 body height Observation."/>
            <expression value="Bundle.entry.resource.where($this is Observation).where(meta.profile = 'http://nictiz.nl/fhir/StructureDefinition/nl-core-BodyHeight').count() = 1"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
    
    <action>
        <assert>
            <description value="Check that the transaction Bundle contains exactly 1 body weight Observation."/>
            <expression value="Bundle.entry.resource.where($this is Observation).where(meta.profile = 'http://nictiz.nl/fhir/StructureDefinition/nl-core-BodyWeight').count() = 1"/>
            <warningOnly value="{$warningOnly}"/>
        </assert>
    </action>
</nts:component>
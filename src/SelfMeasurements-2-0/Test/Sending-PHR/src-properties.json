
  { "goal" : "${goal}",
    "fhirVersion" : "${fhir.version}",
    "informationStandard" : "${informationStandard}",
    "usecase" : "${usecase}",
    "role" : 
    { "name" : "Sending-PHR",
      "description" : "PHR that sends information to a receiving " },

    "serverAlias" : "${serverAlias}" }